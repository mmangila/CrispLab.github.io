import React from 'react';
import { BaseProps } from './types';
interface Feat {
    start: number;
    end: number;
    type: string;
}
interface ParentFeat extends Feat {
    strand?: number;
    subfeatures?: Feat[];
}
export declare const SequencePanel: React.ForwardRefExoticComponent<{
    sequence: {
        seq: string;
        upstream: string;
        downstream: string;
    };
    feature: ParentFeat;
    mode: string;
} & React.RefAttributes<HTMLDivElement>>;
export default function SequenceFeatureDetails(props: BaseProps): JSX.Element;
export {};
