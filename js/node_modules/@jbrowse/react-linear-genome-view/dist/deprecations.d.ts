import { ReactNode } from 'react';
import { Theme, ThemeOptions } from '@material-ui/core/styles';
export declare function createJBrowseTheme(theme?: ThemeOptions): Theme;
export declare function ThemeProvider({ _theme, children, }: {
    _theme: Theme;
    children: ReactNode;
}): JSX.Element;
